# Kidney Transplant KPI Agent - Environment Configuration
# Copy this file to .env and fill in your secure values

# HMAC-SHA256 Audit Trail Secret Key
# Generate a secure key with: python -c "import secrets; print(secrets.token_hex(32))"
AUDIT_SECRET_KEY=change-me-to-a-secure-random-key

# Model Provider (mock, ollama, claude, openai)
MODEL_PROVIDER=mock
